apiVersion: v1
kind: Service
metadata:
  name: anomascan
  namespace: spin-core
spec:
  type: NodePort  # browser must reach remoteEntry.js for Module Federation
  selector:
    app: anomascan
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30003
