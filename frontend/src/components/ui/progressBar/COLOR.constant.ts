import type { ProgressColor } from './ProgressColor.type'

export const COLOR: Record<ProgressColor, string> = {
  blue:  'bg-blue-500',
  green: 'bg-green-500',
  amber: 'bg-amber-500',
  red:   'bg-red-500',
}
